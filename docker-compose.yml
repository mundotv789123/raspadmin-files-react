version: '3.1'
services:
  frontend:
    image: 'mundotv789123/raspadmin:front'
    ports:
      - '8080:80'

  backend:
    image: 'mundotv789123/raspadmin:java'
    volumes:
      - './data:/app/data'
